Fuel:
  BlazePowder:
    Material: 'BLAZE_POWDER'
    FuelAmount: 20
Recipies:
  Speed:
    BrewableItems:
      - 'SUGAR'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'SPEED'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Slowness:
    BrewableItems:
      - 'SUGAR'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'SPEED'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Poison:
    BrewableItems:
      - 'SPIDER_EYE'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'POISON'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Heal:
    BrewableItems:
      - 'GLISTENING_MELON'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'HEAL'
    BaseDuration: 0
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Harming:
    BrewableItems:
      - 'FERMENTED_SPIDER_EYE'
    ParentPotion:
      - 'POTION:HEAL'
    BaseEffect: 'DAMAGE'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Strength:
    BrewableItems:
      - 'BLAZE_POWDER'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'DAMAGE'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Weakness:
    BrewableItems:
      - 'FERMENTED_SPIDER_EYE'
    ParentPotion:
      - 'Potion:Damage'
    BaseEffect: 'WEAKNESS'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  WaterBreathing:
    BrewableItems:
      - 'PUFFER_FISH'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'WATER_BREATHING'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  NightVision:
    BrewableItems:
      - 'GOLDEN_CARROT'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'NIGHT_VISION'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Invisibility:
    BrewableItems:
      - 'FERMENTED_SPIDER_EYE'
    ParentPotion:
      - 'Potion:NIGHT_VISION'
    BaseEffect: 'INVISIBILITY'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Regeneration:
    BrewableItems:
      - 'GHAST_TEAR'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'REGENERATION'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  FireResistance:
    BrewableItems:
      - 'MAGMA_CREAM'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'FIRE_RESISTENCE'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Jump:
    BrewableItems:
      - 'RABBITS_FOOT'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'JUMP'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  SlowFall:
    BrewableItems:
      - 'PHANTOM_MEMBRANE'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'SLOW_FALLING'
    BaseDuration: 180
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Haste:
    BrewableItems:
      - 'CARROT'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'HASTE'
    BaseDuration: 180
    ArcaneBrewingTier: 1
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  MiningFatigue:
    BrewableItems:
      - 'SLIME_BALL'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'MINING_FATIGUE'
    BaseDuration: 180
    ArcaneBrewingTier: 1
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  DolphinsGrace:
    BrewableItems:
      - 'LILY_PAD'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'DOLPHINS_GRACE'
    BaseDuration: 15
    ArcaneBrewingTier: 2
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 20'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Absorption:
    BrewableItems:
      - 'NETHER_QUARTZ'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'ABSORPTION'
    BaseDuration: 180
    ArcaneBrewingTier: 2
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  HealthBoost:
    BrewableItems:
      - 'APPLE'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'HEALTH_BOOST'
    BaseDuration: 180
    ArcaneBrewingTier: 3
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Hunger:
    BrewableItems:
      - 'ROTTEN_FLESH'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'HUNGER'
    BaseDuration: 180
    ArcaneBrewingTier: 3
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Blindness:
    BrewableItems:
      - 'INC_SAC'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'BLINDNESS'
    BaseDuration: 180
    ArcaneBrewingTier: 4
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Nausea:
    BrewableItems:
      - 'BROWN_MUSHROOM'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'NAUSEA'
    BaseDuration: 180
    ArcaneBrewingTier: 4
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Saturation:
    BrewableItems:
      - 'EMERALD_BLOCK'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'SATURATION'
    BaseDuration: 180
    ArcaneBrewingTier: 5
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Decay:
    BrewableItems:
      - 'GOLD_BLOCK'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'WITHER'
    BaseDuration: 180
    ArcaneBrewingTier: 5
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'
  Resistance:
    BrewableItems:
      - 'IRON_BLOCK'
    ParentPotion:
      - 'Potion:Awkward'
    BaseEffect: 'RESISTANCE'
    BaseDuration: 180
    ArcaneBrewingTier: 5
    #How much fuel needs to be used in order to
    FuelConsumption: 1
    DurationAmplifier:
      #How many times redstone can be applied
      MaxAmount: 1
      #When using redstone, how should the duration be modified
      DurationModification: '(duration * 2) + 180'
      #How much fuel should be consumed when redstone is used
      FuelConsumption: 2
      #If the effect amplifier should be decremented by 1
      LowerEffectOnUse: true
    EffectAmplifier:
      #How many times glowstone can be applied
      MaxAmount: 1
      #When using glowstone, how should the effect be modified
      EffectModification: 'effect + 1'
      #How much fuel should be consumed when glowstone is used
      FuelConsumption: 2
      LowerDurationOnUse: true
      #If the duration is already not extended, then how much should the duration be decreased by
      LowerDurationIfNotExtended: 'duration/2'
    Splash:
      CanSplash: true
      FuelConsumption: 2
      DurationModifier: 'duration * .66'
    Lingering:
      CanLinger: true
      FuelConsumption: 5
      DurationModifier: 'duration * .66'